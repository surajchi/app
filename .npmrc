# Expo / React Native / Metro assume a flat node_modules. pnpm's default isolated
# symlink layout breaks the Expo CLI and Metro resolution, so use a hoisted layout
# for this workspace.
node-linker=hoisted
